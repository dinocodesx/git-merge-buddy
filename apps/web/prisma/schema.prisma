generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model WaitlistEntry {
  id          String   @id @default(uuid())
  email       String   @unique
  reason      String?
  usage       String?
  improvement String?
  pricing     String?
  createdAt   DateTime @default(now())
}
